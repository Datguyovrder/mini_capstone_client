 <% @products.each do |product| %>
   <h2>Name: <%= product["name"] %></h2>
   <h3>Price: <%= number_to_currency(product["price"]) %><h3>
   <h4>Description: <%= product["description"] %></h4>
   <h4>Image URL: <%= product["images"] %></h4>
   <br>
 <% end %> 